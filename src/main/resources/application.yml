server:
  port: 8091
  error:
    include-message: always
    include-binding-errors: always
#  jdbc:postgresql://localhost:5432/portfolio_mottu_db
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/challenge_mottu
    username: postgres
    password: admin

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

